<%= link_to(
  "Remove #{@queue_type.titleize} Requests", 
  destroy_all_current_queue_type_requests_path(queue_type: @queue_type),
  method: :delete,
  data: {confirm: "Are you sure?"},
  class:'btn btn-danger')%>
&nbsp;&nbsp;
<%= link_to(
  "Remove All Requests", 
  destroy_all_requests_path,
  method: :delete,
  data: {confirm: "Are you sure?"}, 
  class:'btn btn-danger') %>
